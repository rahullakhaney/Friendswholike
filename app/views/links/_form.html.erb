<div class="row">
  <div class="col-md-6 col-md-offset-3">
    
<%= form_for(@link) do |f| %>
  <% if @link.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@link.errors.count, "error") %> prohibited this link from being saved:</h2>

      <ul>
      <% @link.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="form-group">
    <a href="http://findmyfbid.com" target="_blank">Find Facebook ID Here</a>
  </div>
  <div class="form-group">
    <%= f.label :url %>
    <%= f.text_field :url, placeholder: "URL", class: "form-control", autofocus: true %>
  </div>

  
    <div class="form-group">
      <%= f.label :title %>
      <%= f.text_field :title, placeholder: "Title", class: "form-control" %>
    </div>

    <div class="form-group">
      <%= f.label :description %>
      <%= f.text_field :description, placeholder: "Description", class: "form-control" %>
    </div>

    <div class="form-group">
      <a href="http://makeagif.com/">Fing GIF Images Here</a>
    </div>

    <div class="form-group">
      <%= f.label :image %>
      <%= f.text_field :image, placeholder: "Image URL", class: "form-control" %>
    </div>
  

  <div class="form-group">
    <%= f.button "Submit", class: "btn btn-block btn-primary", data: {disable_with: "Submitting..."} %>
  </div>
<% end %>

  </div>
</div>